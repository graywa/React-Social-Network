import React from "react";
import classes from "./Settings.module.css";
import {countUsers} from "../Users/countUser";

const Settings = (props) => {
    return <div>
        Settings
    </div>
}

export default Settings;